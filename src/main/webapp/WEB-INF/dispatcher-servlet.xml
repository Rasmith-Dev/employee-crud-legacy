<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Enable Spring MVC features (handler mapping, etc.) -->
    <mvc:annotation-driven />

    <!-- View Resolver -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- DataSource -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver" />
        <property name="url" value="jdbc:mysql://localhost:3306/employee_db" />
        <property name="username" value="employee_user" />
        <property name="password" value="password" />
    </bean>

    <!-- JdbcTemplate -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <!-- DAO -->
    <bean id="employeeDAO" class="com.example.employee.dao.EmployeeDAO">
        <constructor-arg ref="jdbcTemplate" />
    </bean>

    <!-- Controller with setter injection -->
    <bean id="employeeController" class="com.example.employee.controller.EmployeeController">
        <property name="employeeDAO" ref="employeeDAO" />
    </bean>

    <!-- Handler Mapping -->
    <bean class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="order" value="1" />
        <property name="urlMap">
            <map>
                <entry key="/" value-ref="employeeController" />
                <entry key="/employees" value-ref="employeeController" />
                <entry key="/employee/form" value-ref="employeeController" />
                <entry key="/employee/save" value-ref="employeeController" />
                <entry key="/employee/edit" value-ref="employeeController" />
                <entry key="/employee/delete" value-ref="employeeController" />
            </map>
        </property>
    </bean>

    <!-- Handler Adapter -->
    <bean class="org.springframework.web.servlet.mvc.SimpleControllerHandlerAdapter" />

</beans>
